# NOTE: `databricks sync` applies BOTH .gitignore and this file — this does NOT
# override .gitignore. Because .gitignore excludes frontend/dist/, deploy.py
# temporarily strips that line during sync so the built frontend uploads.
frontend/node_modules/
__pycache__/
*.pyc
.venv/
.databricks/
.genie_create.json
